import { ElementPaddingDirective } from './element-padding.directive';

describe('ElementPaddingDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementPaddingDirective();
    expect(directive).toBeTruthy();
  });
});
